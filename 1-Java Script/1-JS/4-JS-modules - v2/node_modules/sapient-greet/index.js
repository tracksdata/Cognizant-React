console.log("-greet/index.js-");

// (function (exports, require, module, __filename, __dirname) {

var message = "good morning";





function greet(lang) {
    if (lang === "en") {
        const en = require('./en');
        console.log(`${en()} , ${message}`); return
    }
    if (lang === "es") {
        const es = require('./es');
        console.log(`${es()} , ${message}`);
        return;
    }
    console.log("give me your language, to greet you");
}

function syaName() {
    console.log('im ..');
}

module.exports = {
    wish: greet,
    syaName
}


// })()
